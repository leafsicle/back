<%= form_with(model: frequency, local: true) do |form| %>
  <% if frequency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(frequency.errors.count, "error") %> prohibited this frequency from being saved:</h2>

      <ul>
      <% frequency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :duration %>
    <%= form.text_field :duration %>
  </div>

  <div class="field">
    <%= form.label :days %>
    <%= form.number_field :days %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
