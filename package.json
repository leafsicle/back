{
	"private": true,
	"scripts": {
		"build": "cd client && npm install && npm run build & cd ..",
		"deploy": "cp -a client/build/. public/",
		"postinstall": "cd client && npm install && npm run build && npm run deploy && echo 'client built!'"
	}
}
